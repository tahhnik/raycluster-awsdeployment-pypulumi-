name: aws-raycluster-pulumi
runtime:
  name: python
  options:
    virtualenv: venv
description: Automating the deployment of Ray clusters on AWS EC2 instances wt
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
